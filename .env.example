# ============== Vercel (Project → Settings → Environment Variables) ==============
# Required for Online 1vs1 (API routes use Supabase).

# Supabase project URL (Dashboard → Project Settings → API)
SUPABASE_URL=https://xxxxx.supabase.co

# Supabase service role key (Dashboard → Project Settings → API → service_role, secret)
# Do not expose in frontend. Only used by API routes.
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Optional: override API base URL for frontend (default: same origin /api)
# VITE_API_URL=https://your-vercel-app.vercel.app
